---

# Installing dependencies:
- brewfile:
    - brew/Brewfile

- shell:
  - command: bash -l './scripts/omf-install.sh'
    description: Installing vscode extensions
    stdout: true
    stderr: true

